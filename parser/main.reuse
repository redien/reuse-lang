
(def reuse-main (stdin)
     ((pipe parse
            (fn (expressions) (sexps-to-definitions expressions (parser-context-new)))
            parser-context-definitions
            definitions-to-sexps
            stringify)
          stdin))
