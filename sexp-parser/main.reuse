
(def reuse-main (stdin)
     (match (parse stdin)
            (Result (Pair symbols expressions))
                 (stringify symbols expressions)
            (Error _)
                 (string-of-char 45)))
