
(def reuse-main (stdin)
     (match (parse (dictionary-empty) stdin)
            (Result (Pair _ expressions))
                 (stringify expressions)
            (Error _)
                 (string-of-char 45)))
