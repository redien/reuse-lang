
(export to_reuse (definitions _)
        ((pipe local-transforms
               (list-map (result-map definition-to-sexp))
               result-concat
               (result-map stringify))
            definitions))
