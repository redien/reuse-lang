
(export to_reuse (definitions _)
        ((pipe local-transforms
               (list-map (result-first definition-to-sexp))
               result-concat
               (result-first stringify))
            definitions))
